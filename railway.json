{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "gunicorn --chdir backend --bind 0.0.0.0:$PORT --timeout 900 app:app",
    "restartPolicyMaxRetries": 5
  }
}
